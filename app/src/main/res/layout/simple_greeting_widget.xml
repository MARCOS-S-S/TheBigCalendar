<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:orientation="vertical"
    android:padding="12dp"
    android:gravity="center">

    <!-- Saudação -->
    <TextView
        android:id="@+id/simple_widget_greeting"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Bom dia"
        android:textColor="@color/widget_text_color_fixed"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp"
        android:gravity="center" />

    <!-- Dia da semana -->
    <TextView
        android:id="@+id/simple_widget_day_of_week"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Segunda-feira"
        android:textColor="@color/widget_text_color_fixed"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_marginBottom="4dp"
        android:gravity="center" />

    <!-- Dia do mês -->
    <TextView
        android:id="@+id/simple_widget_day_of_month"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="26 de agosto"
        android:textColor="@color/widget_text_color_fixed"
        android:textSize="14sp"
        android:layout_marginBottom="8dp"
        android:gravity="center" />

    <!-- Botão de refresh -->
    <ImageButton
        android:id="@+id/simple_widget_refresh_button"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@android:drawable/ic_popup_sync"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:contentDescription="Refresh Widget"
        android:padding="4dp" />

</LinearLayout>

